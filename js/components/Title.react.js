var React = require('react');

var Title = React.createClass({

    render: function() {
        return (
            <div className="title">
                Giddit
            </div>
        );
    }
});

module.exports = Title;
